from datetime import date

# today = date.today()

# print(today)   # uses str(today)
# print()
# print(repr(today))  # uses repr(today)
# print()
# print(f"{today = }")  # also uses repr(today)
# print()
# print(f"{today = !s}")  # also uses repr(today)

poem = """I think that I shall never see
a poem lovely as a tree"""

print(poem)   # uses str(today)
print()
print(repr(poem))  # uses repr(today)
print()
print(f"{poem = }")  # also uses repr(today)
print()
print(f"{poem = !s}")  # also uses repr(today)
